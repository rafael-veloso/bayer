{
  "configurations": [
    {
      "type": "pwa-chrome",
      "request": "launch",
      "name": "Docker angular",
      "url": "http://localhost:4200",
      "webRoot": "${workspaceFolder}",
      "sourceMapPathOverrides": {
        "webpack:/*": "${webRoot}/*"
      }
    }
  ]
}
